version: '3'
services:
  sc-generator:
    build: .
    container_name: sc-generator
    volumes:
      - ./sol:/sol
      - ./solana-config:/root/.config/solana
    stdin_open: true
    tty: true
    command: ["tail", "-f", "/dev/null"]
